import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#726630',
    },
  },
  // status: {
  //   danger: orange[500],
  // },
})

export default theme
